<?xml version="1.0" encoding="utf-8"?>
<!-- Saldo de la cuenta con la que operas.
     Tres líneas y ni una más: de qué es, cuánto hay, y qué clase de
     cuenta es. En un widget de una fila no cabe otra cosa, y lo que no
     cabe se lee mal, que es peor que no estar. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/wsRaiz"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_fondo"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- El color de la cuenta. Va con el nombre al lado, nunca solo:
             el color identifica, pero quien lo dice es el nombre. -->
        <TextView
            android:id="@+id/wsPunto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="●"
            android:textSize="13sp"
            android:layout_marginEnd="6dp"
            android:textColor="@color/colorAccent" />

        <TextView
            android:id="@+id/wsNombre"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/widgetTinta" />
    </LinearLayout>

    <TextView
        android:id="@+id/wsSaldo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/widgetTinta" />

    <TextView
        android:id="@+id/wsTipo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="@color/widgetTintaSuave" />
</LinearLayout>
