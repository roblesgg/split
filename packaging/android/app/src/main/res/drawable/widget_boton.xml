<?xml version="1.0" encoding="utf-8"?>
<!-- El círculo del widget de apuntar. Degradado como el botón de la app:
     el mismo gesto, el mismo color. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="oval">
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/widgetBotonAlto"
        android:endColor="@color/widgetBotonBajo" />
</shape>
