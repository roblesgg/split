/* ============================================================
   split — espacio común del motor de gráficos

   Cada tipo de gráfico vive en su archivo y cuelga aquí lo suyo. La API
   que ve la app es window.Charts, que se monta al final en js/charts.js.
   ============================================================ */

window.Graficos = {};
